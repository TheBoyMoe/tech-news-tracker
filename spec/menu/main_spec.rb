require "spec_helper"
require 'stringio'


RSpec.describe NewsTracker::Menu::Main do

  context 'process the user input' do

    it "the user selects an article list, switch to 'NewsTracker::Menu::List'" do

    end

    it "the user selects archive, switch to 'NewsTracker::Menu::Archive'" do

    end

    it "the user selects 'exit', terminate app" do

    end

    it "the user enters an unknown command, stay on the same menu" do

    end

  end

end
